2026-03-26
1. 检查 `problems/cvrp_hgs`、`cfg/problem/cvrp_hgs.yaml`、`prompts/cvrp_hgs`、`reevo.py`、`main.py` 的现状，确认需要在不修改 `prompts/cvrp_hgs` 的前提下补齐新问题适配层。
2. 确认 `problems/cvrp_hgs` 已包含：
   - 58 个 `CVRP X` 基准实例；
   - 原始 HGS 运行结果目录 `data/20260320_110714_500`；
   - 原始 `selective_route_exchange.cpp` 备份 `data/selective_route_exchange.original.cpp`；
   - 可用于替换和编译的 `pyvrp_rep/pyvrp/cpp/crossover/selective_route_exchange.cpp`。
3. 确认 `prompts/cvrp_hgs/utils/*` 是 C++17 专用 prompt 模板，必须直接使用，不能回退到 `prompts/common/*` 的 Python 模板。
4. **！！！**新增 `problems/__init__.py` 与 `problems/cvrp_hgs/__init__.py`，让 `main.py` 可以最小代价导入 `cvrp_hgs` 专用 `ReEvo` 子类。
5. **！！！**新增 `problems/cvrp_hgs/code_extraction.py`，专门从 LLM 响应或原始文件中提取完整 `C++17 selective_route_exchange.cpp`。
6. **！！！**新增 `problems/cvrp_hgs/anti_plagiarism.py`，基于标准化后的代码序列相似度做 `0.95` 阈值检查。
7. **！！！**新增 `problems/cvrp_hgs/reevo_hgs.py`，实现 `CVRPHGSReEvo` 子类，复用 ReEvo 主流程但改用：
   - `prompts/cvrp_hgs/utils/*` 的 C++ prompt；
   - 专用 C++ 代码抽取；
   - 唯一候选文件路径传给 `problems/cvrp_hgs/eval.py`。
8. **！！！**新增 `problems/cvrp_hgs/eval.py`，负责：
   - 读取候选 `selective_route_exchange.cpp`；
   - 调用 `anti_plagiarism.py`；
   - 将候选代码写入 `pyvrp_rep` 的 `selective_route_exchange.cpp` 与 `selective_route_exchange_candidate.cpp`；
   - 对 PyVRP 做增量编译；
   - 按 `cpu核心数 // 2` 并行验证实例；
   - 训练模式输出平均 `obj_ratio_vs_baseline` 作为 ReEvo 目标值；
   - 验证模式输出 58 实例汇总指标。
9. **！！！**修改 `cfg/problem/cvrp_hgs.yaml`，补充 `dataset_dir`、`baseline_dir`、`pyvrp_root`、`plagiarism_threshold=0.95`、`proxy_instance_count`、`parallel_workers_divisor` 等配置字段。
10. **！！！**修改 `main.py`，仅增加一个按 `problem_name == "cvrp_hgs"` 切换到 `CVRPHGSReEvo` 的分支，其他逻辑保持不变。
11. 修正 `problems/cvrp_hgs/eval.py` 两个关键细节：
   - `ProcessPoolExecutor` 改为顶层可序列化 worker；
   - seed 基线与最终 `gpt.py` 回放不启用拒绝式反抄袭，但仍记录相似度。

12. 安装 `meson`、`docblock`、`vrplib`、`matplotlib` 和新版 `pybind11==2.13.6`，并修正 `eval.py` 的构建环境，使 Meson 优先使用 `.venv` 内的 pybind11。
13. 清理被系统旧版 pybind11 污染过的 `problems/cvrp_hgs/pyvrp_rep/build-reevo-cvrp-hgs-py311`，准备重新配置构建。

14. 使用原始 `selective_route_exchange.cpp` 作为 seed 候选运行 `problems/cvrp_hgs/eval.py train`，完成 Python 3.11 版 PyVRP 构建、候选 C++ 增量编译和 12 个代理实例并行验证，链路跑通。
15. seed 代理集结果：`avg_obj_ratio_vs_baseline = 5.833333333333333`，`feasible = 12/12`，`improved = 1/12`。
16. 继续启动本地 `vllm` 服务，并运行最小端到端命令：
   - `python main.py problem=cvrp_hgs llm_client=local init_pop_size=1 pop_size=1 max_fe=2 timeout=1800`
17. 端到端运行结果：
   - `Iteration 0` 的 seed 候选成功进入代理集评估，得到 `Objective value = -3.75`；
   - `Iteration 1` 的首个 LLM 候选被成功抽取、写入、触发增量编译，但编译失败，故该候选目标值记为 `inf`；
   - 失败原因为候选代码里 `aMoveDelta` 和 `bMoveDelta` 使用了无符号类型，却与 `0` 做 `< 0` 比较，在 `-Werror` 下被 C++ 编译器拒绝。
18. 最小 `main.py` 运行已完整结束，并自动执行 58 个实例全量验证。当前最佳代码仍是原始 seed，对应 58 实例汇总结果：
   - `Average objective ratio vs baseline obj = 35.137931`
   - `Feasible solutions = 58/58`
   - `Improved over baseline = 0/58`
   - 说明 `cvrp_hgs` 问题接入、反抄袭、代码抽取、增量编译、并行验证、最终验证全链路已经可用，后续只需要增大搜索预算继续演化。
19. 端到端验证结束后已停止本地 `vllm` 服务，释放 GPU 显存；后续继续搜索时按现有本地脚本重新启动即可。
20. **！！！**修改 `problems/cvrp_hgs/eval.py`，在完整代理集/全量评估前增加 `smoke test`：
   - 固定先测一个代表实例；
   - 若 120 秒内仍未结束，则立即终止并输出 `inf`（训练模式）；
   - 若正常结束，则继续进入原有并行评估流程。
21. **！！！**修改 `cfg/problem/cvrp_hgs.yaml`，加入 `smoke_test_instance: X-n101-k25.vrp` 与 `smoke_test_timeout: 120` 配置。
22. 检查当前 `problems/cvrp_hgs/eval.py` 的锁范围与 `pyvrp_rep` 的插件加载逻辑，确认旧实现虽然支持单候选内部按 `cpu核心数 // 2` 并行跑实例，但不同候选之间仍共享：
   - 同一个 `pyvrp_rep/pyvrp/cpp/crossover/selective_route_exchange_candidate.cpp`；
   - 同一个 `build-reevo-cvrp-hgs-py311`；
   - 同一个 `pyvrp.crossover._srex_plugin` 安装目标；
   - 因此需要额外做候选级 sandbox 才能真正并行化。
23. 查阅 `pyvrp_rep/pyvrp/crossover/selective_route_exchange.py`、`pyvrp_rep/meson.build`、`pyvrp_rep/pyvrp/cpp/crossover/srex_plugin_bindings.cpp`，确认仓库已经具备：
   - 候选插件 `_srex_plugin`；
   - `PYVRP_SREX_USE_PLUGIN` 环境变量切换；
   - 单独的 `selective_route_exchange_candidate.cpp` 编译目标；
   - 但当前 `eval.py` 还没有把这些能力用于“候选间隔离”。
24. 第一次并行化方案尝试：
   - 先锁住共享基础 build；
   - 再复制一份已编好的 `pyvrp_rep` 到临时目录；
   - 在临时目录内做候选增量编译和评估。
25. **！！！**第一次修改 `problems/cvrp_hgs/eval.py`：
   - 新增 `tempfile`；
   - 新增 `sandbox_problem_paths(...)`；
   - 新增 `clone_pyvrp_sandbox(...)`；
   - 将候选编译和评估路径重定向到 `/tmp/cvrp-hgs-pyvrp-*/pyvrp_rep`；
   - `incremental_build_candidate(...)` 改成只写 sandbox 内的 `candidate_cpp`。
26. 运行语法检查：
   - `/home/zguo/Coding/reevo_VRP/.venv/bin/python -m py_compile /home/zguo/Coding/reevo_VRP/problems/cvrp_hgs/eval.py`
27. 第一次并行验证命令：
   - `/home/zguo/Coding/reevo_VRP/.venv/bin/python -u /home/zguo/Coding/reevo_VRP/problems/cvrp_hgs/eval.py 58 /home/zguo/Coding/reevo_VRP train /home/zguo/Coding/reevo_VRP/outputs/cvrp_hgs-hgs/2026-03-26_12-34-14/problem_iter12_candidate12_selective_route_exchange.cpp`
   - `/home/zguo/Coding/reevo_VRP/.venv/bin/python -u /home/zguo/Coding/reevo_VRP/problems/cvrp_hgs/eval.py 58 /home/zguo/Coding/reevo_VRP train /home/zguo/Coding/reevo_VRP/outputs/cvrp_hgs-hgs/2026-03-26_12-34-14/problem_iter13_candidate2_selective_route_exchange.cpp`
28. 第一次并行验证观察结果：
   - 两个 `eval.py` 确实同时启动了各自的 `meson compile` 与 `ninja`；
   - 但 Meson 打印的 `Source dir` 仍指向仓库内原始 `pyvrp_rep`；
   - 说明“复制旧 build 目录”会把 Meson 的源目录绑定也带过去，不是真正隔离。
29. 基于第一次验证结果，确认必须进一步修正为：
   - 复制源码树；
   - 不复制 `build-*`；
   - 每个候选在自己的 sandbox 中重新创建 build 目录。
30. **！！！**第二次修改 `problems/cvrp_hgs/eval.py`：
   - 移除候选级共享 `build.lock` 依赖；
   - `clone_pyvrp_sandbox(...)` 增加忽略项：`build-*`、`docs`、`examples`、`tests`；
   - 让每个候选在独立 `/tmp/.../pyvrp_rep` 下重新执行 `buildtools/build_extensions.py`；
   - 保证 `_srex_plugin`、`_crossover`、`_pyvrp` 等安装到各自独立 sandbox。
31. 再次运行语法检查：
   - `/home/zguo/Coding/reevo_VRP/.venv/bin/python -m py_compile /home/zguo/Coding/reevo_VRP/problems/cvrp_hgs/eval.py`
32. 第二次并行验证命令：
   - `/home/zguo/Coding/reevo_VRP/.venv/bin/python -u /home/zguo/Coding/reevo_VRP/problems/cvrp_hgs/eval.py 58 /home/zguo/Coding/reevo_VRP train /home/zguo/Coding/reevo_VRP/outputs/cvrp_hgs-hgs/2026-03-26_12-34-14/problem_iter12_candidate12_selective_route_exchange.cpp`
   - `/home/zguo/Coding/reevo_VRP/.venv/bin/python -u /home/zguo/Coding/reevo_VRP/problems/cvrp_hgs/eval.py 58 /home/zguo/Coding/reevo_VRP train /home/zguo/Coding/reevo_VRP/outputs/cvrp_hgs-hgs/2026-03-26_12-34-14/problem_iter13_candidate2_selective_route_exchange.cpp`
33. 第二次并行验证关键证据：
   - 候选 A 的 Meson 输出：
     - `Source dir: /tmp/cvrp-hgs-pyvrp-nrjchfix/pyvrp_rep`
     - `Build dir: /tmp/cvrp-hgs-pyvrp-nrjchfix/pyvrp_rep/build-reevo-cvrp-hgs-py311`
   - 候选 B 的 Meson 输出：
     - `Source dir: /tmp/cvrp-hgs-pyvrp-6u4oxslt/pyvrp_rep`
     - `Build dir: /tmp/cvrp-hgs-pyvrp-6u4oxslt/pyvrp_rep/build-reevo-cvrp-hgs-py311`
   - 进程表同时出现：
     - 两条 `eval.py`
     - 两条 `meson compile`
     - 两条 `ninja`
   - 证明不同候选已经进入不同 sandbox，能够真正并行编译和评估。
34. 在确认关键证据后，手动中止这两条验证进程，避免继续占用大量 CPU 资源。
35. 当前并行化后的结论：
   - 单个候选内部仍然保留实例级并行；
   - 不同候选之间也已经具备真正并行的 sandbox；
   - 后续若重新运行 `population=16` 的长程 evolve，可望显著减少“候选排队等待同一 build/plugin”的时间。
